/**
 * Adiciona os event listeners aos botões do mapa, bússola e mochila.
 * @function addEventListeners
 * @memberof window
 */
function addEventListeners() {
    document.getElementById('mapButton').addEventListener('click', openMap);
    document.getElementById('compassButton').addEventListener('click', openCompass);
    document.getElementById('backpackButton').addEventListener('click', openBackpack);
}

/**
 * Função para abrir o mapa.
 * @function openMap
 * @memberof window
 */
function openMap() {
    // Código para abrir o mapa
}

/**
 * Função para abrir a bússola.
 * @function openCompass
 * @memberof window
 */
function openCompass() {
    // Código para abrir a bússola
}

/**
 * Função para abrir a mochila.
 * @function openBackpack
 * @memberof window
 */
function openBackpack() {
    // Código para abrir a mochila
}

addEventListeners();